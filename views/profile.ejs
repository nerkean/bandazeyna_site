<% 
    const assetMap = {
        'profile_frame_veteran': 'veteran',
        'profile_frame_laurel_blue': 'laurel_blue',
        'profile_frame_patterned_bronze': 'patterned_bronze',
        'premium_avatar_frame_gold_plated': 'premium_gold_plated',
        'avatar_frame_azure_spark': 'azure_spark',
        'hween_frame_web': 'halloween_web',
        'profile_bg_legend': 'legend',
        'profile_bg_stars_dark': 'stars_dark',
        'premium_card_bg_galaxy': 'premium_galaxy',
        'hween_bg_pumpkins': 'halloween_pumpkins'
    };

    function getFrameUrl(id) {
        if (!id) return null;
        let filename = assetMap[id];
        if (!filename) filename = id.replace('profile_frame_', '');
        return `/assets/thumbnails/frames/${filename}.webp`; 
    }

    function getAssetUrl(id, type) {
        if (!id) return null;
        let filename = assetMap[id];
        if (!filename) filename = id.replace('profile_frame_', '').replace('profile_bg_', '');
        return `/assets/${type}/${filename}.webp`;
    }

    let headerBgUrl = '/assets/backgrounds/profile_template.png'; 
    let bannerStyle = ''; 
    let headerClass = 'profile-header';

    if (profile.activeCardBgId) {
        let bgName = assetMap[profile.activeCardBgId];
        if (!bgName) bgName = profile.activeCardBgId.replace('profile_bg_', '');
        headerBgUrl = `/assets/backgrounds/${bgName}.webp`;
        bannerStyle = `background-image: url('${headerBgUrl}');`;
    } else {
        headerClass += ' default-bg';
    }
%>
<!DOCTYPE html>
<html lang="ru">
<head>
    <%- include('partials/head', { title: 'Профиль | ' + targetUser.username }) %>
    <link rel="stylesheet" href="/css/profile.css">
    <link rel="stylesheet" href="/css/modals.css">
    <link rel="preload" as="image" href="<%= headerBgUrl %>" fetchpriority="high">
</head>
<body>
    <%- include('partials/navbar') %>

    <div class="profile-wrapper">
        
        <header class="<%= headerClass %>" style="<%= bannerStyle %>" role="banner">
            <div class="profile-header-overlay">
                
              <div class="avatar-wrapper">
                    <% if (profile.activeAvatarFrameId) { %>
                        <img src="<%= getFrameUrl(profile.activeAvatarFrameId) %>" class="avatar-frame" alt="Рамка">
                    <% } %>
                    
                    <img src="<%= targetUser.avatar ? `/img/proxy/avatar/${targetUser.id}/${targetUser.avatar}` : '/assets/img/avatars/default_avatar.png' %>" 
                         class="avatar-big" alt="Аватар">
                </div>
                
                <div class="profile-identity">
                    <h1><%= targetUser.username %></h1>
                    <% if (profile.activeTitle) { %>
                        <span class="user-title"><%= profile.activeTitle %></span>
                    <% } %>
                    
                    <div class="badges">
                        <% if (profile.premiumType) { %>
                            <a href="/shop?category=ПремиумView" class="badge premium"><i class="fas fa-gem"></i> Premium</a>
                        <% } %>
                        <% if (profile.isTaxDelinquent) { %>
                            <span class="badge tax-warning"><i class="fas fa-exclamation-triangle"></i> Должник</span>
                        <% } %>
                        <span class="badge rep">
                            <i class="fas fa-thumbs-up"></i> Репутация: <%= profile.reputation %>
                        </span>
                    </div>
                </div>

                <% if (isOwner) { %>
                    <button class="btn-edit" onclick="openEditModal()">
                        <i class="fas fa-pen"></i> Настроить титул
                    </button>
                <% } else { %>
                    <a href="/messages/<%= targetUser.id %>" class="btn-edit" style="background: var(--primary); border-color: var(--primary);">
                        <i class="fas fa-envelope"></i> Написать
                    </a>
                <% } %>
            </div>
        </header>

        <div class="dashboard-grid">
            
            <div class="col-left">
                <div class="card glow-card">
                    <h2><i class="fas fa-coins"></i> Финансы <a href="/giveaways" title="Розыгрыши"><i class="fas fa-gift"></i></a></h2>
                    <div class="stat-row"><span>Наличные:</span><span class="val gold"><%= Math.floor(profile.stars).toLocaleString() %> ⭐</span></div>
                    <div class="stat-row"><span>Осколки:</span><span class="val shard"><%= profile.shards %> ✨</span></div>
                    <div class="divider"></div>
                    <div class="stat-row total"><span>Net Worth:</span><span class="val"><%= (typeof netWorth !== 'undefined' ? netWorth : 0).toLocaleString() %> ⭐</span></div>
                </div>

                <div class="card">
                    <h2><i class="fas fa-chart-bar"></i> Активность</h2>
                    <div class="stat-row"><span>Сообщений:</span><span class="val"><%= profile.totalMessages %></span></div>
                    <div class="stat-row"><span>Голос:</span><span class="val"><%= (profile.totalVoiceTime / 3600).toFixed(1) %> ч.</span></div>
                    <div class="stat-row"><span>Вступил:</span><span class="val small"><%= new Date(profile.joinedAt).toLocaleDateString() %></span></div>
                </div>
                
                <div class="card">
                    <h2><i class="fas fa-heart"></i> Семья</h2>
                    <% if (profile.marriedTo && partnerName) { %>
                        <div class="marriage-info">
                            <div class="heart-icon"><i class="fas fa-heart"></i></div>
                            <div>
                                <p>В браке с <strong><%= partnerName %></strong></p>
                                <p class="small-date">с <%= new Date(profile.marriageDate).toLocaleDateString() %></p>
                            </div>
                        </div>
                    <% } else { %>
                        <p class="text-muted">Холост / Не замужем</p>
                    <% } %>
                </div>
            </div>

            <div class="col-center">
                <div class="card graph-card">
                    <h2>Активность (7 дней)</h2>
                    <div class="chart-container">
                        <div id="activityChart"></div>
                    </div>
                </div>

                <div class="card">
                    <h2><i class="fas fa-briefcase"></i> Портфель акций</h2>
                    <% if (typeof portfolioDetails !== 'undefined' && portfolioDetails.length > 0) { %>
                        <table class="simple-table">
                            <thead><tr><th>Тикер</th><th>Шт.</th><th>Цена</th><th>Сумма</th></tr></thead>
                            <tbody>
                                <% portfolioDetails.slice(0, 5).forEach(p => { %>
                                    <tr>
                                        <td class="ticker-cell">$<%= p.ticker %></td>
                                        <td><%= p.quantity %></td>
                                        <td><%= p.currentPrice.toFixed(0) %></td>
                                        <td style="color: #ffd700;"><%= p.value.toFixed(0) %></td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                        <% if (portfolioDetails.length > 5) { %>
                            <div style="text-align: center; margin-top: 10px;">
                                <a href="/market" style="color: var(--primary); font-weight: 700;">Показать все</a>
                            </div>
                        <% } %>
                    <% } else { %>
                        <p class="text-muted">Портфель пуст. <a href="/market" style="color: var(--primary);">Купить акции?</a></p>
                    <% } %>
                </div>
                
                <div class="card">
                    <h2><i class="fas fa-scroll"></i> Активные квесты</h2>
                    <% if (typeof quests !== 'undefined' && quests.length > 0) { %>
                        <div class="quest-list">
                            <% quests.forEach(q => { %>
                                <% if (!q.isCompleted) { %>
                                    <div class="quest-item">
                                        <div class="quest-header">
                                            <span class="quest-name"><%= q.details.name %></span>
                                            <span class="quest-progress"><%= q.progress %> / <%= q.target %></span>
                                        </div>
                                        <div class="progress-bar-bg">
                                            <div class="progress-bar-fill" style="width: <%= (q.progress / q.target) * 100 %>%;"></div>
                                        </div>
                                    </div>
                                <% } %>
                            <% }) %>
                        </div>
                    <% } else { %>
                        <p class="text-muted">Нет активных квестов</p>
                    <% } %>
                </div>
            </div>

            <div class="col-right">
                <div class="card">
                    <h2><i class="fas fa-box-open"></i> Инвентарь</h2>
                    <div class="inventory-grid">
                        <% profile.inventory.slice(0, 12).forEach(item => { 
                             let rarity = 'common';
                             const cat = item.details ? item.details.category : '';
                             if (cat === 'Лутбокс') rarity = 'legendary';
                             else if (cat === 'Украшение профиля') rarity = 'epic';
                             
                             let imgUrl = null;
                             let showEmoji = true;
                             if (cat === 'Украшение профиля' || cat === 'Косметика') {
                                 let fname = assetMap[item.itemId];
                                 if (!fname) fname = item.itemId.replace('profile_frame_', '').replace('profile_bg_', '');
                                 let fld = item.itemId.includes('bg') ? 'backgrounds' : 'frames';
                                 imgUrl = `/assets/thumbnails/${fld}/${fname}.webp`;
                                 showEmoji = false;
                             } else if (item.details && item.details.imageUrl_web) {
                                 imgUrl = item.details.imageUrl_web;
                                 showEmoji = false;
                             }
                        %>
                            <div class="inv-item <%= rarity %>" title="<%= item.details ? item.details.name : item.itemId %>">
                                <div class="inv-icon">
                                    <% if (!showEmoji && imgUrl) { %>
                                        <img src="<%= imgUrl %>" onerror="this.style.display='none'; this.nextElementSibling.style.display='block'">
                                        <div class="inv-emoji" style="display:none"><%= item.details ? item.details.emoji : '📦' %></div>
                                    <% } else { %>
                                        <div class="inv-emoji"><%= item.details ? item.details.emoji : '📦' %></div>
                                    <% } %>
                                </div>
                                <span class="inv-qty">x<%= item.quantity %></span>
                            </div>
                        <% }) %>
                    </div>
                    <% if (isOwner) { %>
                        <a href="/inventory" class="btn-secondary" style="justify-content: center; margin-top: 15px;">Открыть весь</a>
                    <% } %>
                </div>

                <div class="card">
                    <h2><i class="fas fa-trophy"></i> Достижения</h2>
                    <div class="achievements-list">
                        <% if (typeof achievements !== 'undefined' && achievements.length > 0) { %>
                            <% achievements.forEach(ach => { %>
                                <div class="ach-tag" title="<%= ach.details.description %>">
                                    <%= ach.details.medalEmoji || '🏅' %> <%= ach.details.name %>
                                </div>
                            <% }) %>
                        <% } else { %>
                            <p class="text-muted">Нет достижений</p>
                        <% } %>
                    </div>
                </div>
                
                <% if (isOwner) { %>
                    <div class="card danger-zone">
                        <h2><i class="fas fa-user-secret"></i> Личное</h2>
                        <p>Предупреждений: <strong><%= profile.warningsReceived %></strong></p>
                        <p>Мьютов получено: <strong><%= profile.mutesIssued %></strong></p>
                        <% if (profile.taxDue > 0) { %>
                             <p class="tax-alert">Налоговый долг: <%= profile.taxDue %> ⭐</p>
                        <% } %>
                    </div>
                <% } %>
            </div>
        </div> <div class="profile-full-row">
            <div class="card">
                <h2 id="wallHeader"><i class="fas fa-comments"></i> Стена</h2>
                
                <% if (user) { %>
                    <div class="wall-input-box">
                        <% if (user.avatar) { %>
                            <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" class="wall-input-ava" alt="Ваш аватар">
                        <% } else { %>
                            <img src="https://cdn.discordapp.com/embed/avatars/0.png" class="wall-input-ava" alt="Ваш аватар">
                        <% } %>
                        <input type="text" id="wallComment" class="wall-input" placeholder="Оставить комментарий..." maxlength="250" autocomplete="off">
                        <button class="btn-send" onclick="postComment()"><i class="fas fa-paper-plane"></i></button>
                    </div>
                <% } %>

                <div class="wall-list" id="commentsList">
                    <div class="loader-spinner"><i class="fas fa-spinner fa-spin"></i> Загрузка...</div>
                </div>
                <div class="wall-pagination" id="wallPagination" style="display: none;"></div>
            </div>
        </div>

    </div>

    <% if (isOwner) { %>
    <div class="modal-overlay" id="editModal" role="dialog" aria-modal="true">
        <div class="modal-box">
            <div class="modal-icon"><i class="fas fa-id-badge"></i></div>
            <h2 class="modal-title">Настройка Титула</h2>
            
            <div style="margin-bottom: 20px; text-align: center;">
                <% if (profile.inventory.some(i => i.itemId === 'title_token')) { %>
                    <div style="color: #2ecc71; margin-bottom: 5px;">
                        <i class="fas fa-check-circle"></i> <b>Жетон в наличии</b>
                    </div>
                    <small style="color: #aaa;">Смена стоит 1 жетон</small>
                <% } else { %>
                    <div style="color: #ef4444; margin-bottom: 5px;">
                        <i class="fas fa-times-circle"></i> <b>Нет Жетона</b>
                    </div>
                    <small style="color: #aaa;">Купите "Жетон Титула" в <a href="/shop" style="color: var(--primary);">Магазине</a></small>
                <% } %>
            </div>

            <input type="text" id="editTitle" class="modal-input" 
                   placeholder="Введите новый титул (макс. 30)" maxlength="30" 
                   value="<%= profile.activeTitle || '' %>">
            
            <div class="modal-actions">
                <button class="btn-modal btn-cancel" onclick="closeEditModal()">Отмена</button>
                <button class="btn-modal btn-secondary" onclick="clearTitle()">Сбросить</button>
                <button class="btn-modal btn-confirm" onclick="saveTitle()">Сохранить</button>
            </div>
        </div>
    </div>
    <% } %>

    <%- include('partials/footer') %>
    <script nonce="<%= nonce %>" src="https://cdn.jsdelivr.net/npm/apexcharts"></script>

    <script nonce="<%= nonce %>">
        const targetUserId = "<%= targetUser.id %>";
        const historyData = <%- JSON.stringify(profile.dailyActivityHistory || []) %>;
        let currentWallPage = 1;

        document.addEventListener('DOMContentLoaded', () => {
            loadComments(1);
            initChart();
        });

        function openEditModal() { 
            const modal = document.getElementById('editModal');
            if (modal) modal.classList.add('active'); 
        }
        function closeEditModal() { 
            const modal = document.getElementById('editModal');
            if (modal) modal.classList.remove('active'); 
        }

        async function saveTitle() {
            const title = document.getElementById('editTitle').value;
            if(!title.trim()) return alert("Введите текст титула!");

            if (typeof Modal !== 'undefined') {
                const ok = await Modal.confirm("Подтверждение", "Сменить титул? Это стоит 1 Жетон.");
                if(!ok) return;
            }
            sendUpdate(title);
        }

        async function clearTitle() {
            if (typeof Modal !== 'undefined') {
                const ok = await Modal.confirm("Сброс", "Убрать титул? Это бесплатно.");
                if(!ok) return;
            }
            sendUpdate("");
        }

        async function sendUpdate(title) {
            try {
                const res = await fetch('/api/user/update', {
                    method: 'POST',
                    headers: {'Content-Type': 'application/json'},
                    body: JSON.stringify({ activeTitle: title })
                });
                const data = await res.json();

                if (data.success) {
                    if (typeof Modal !== 'undefined') await Modal.alert("Успешно", "Титул обновлен", "success");
                    else alert("Успешно");
                    location.reload();
                } else {
                    if (typeof Modal !== 'undefined') Modal.alert("Ошибка", data.error, "error");
                    else alert(data.error);
                }
            } catch (e) { alert("Ошибка сети"); }
        }

        function initChart() {
            if (typeof ApexCharts === 'undefined') return;
            
            const days = [], msgData = [], voiceData = [];
            
            for (let i = 6; i >= 0; i--) {
                const d = new Date();
                d.setDate(d.getDate() - i);
                days.push(d.toLocaleDateString('ru-RU', { day: 'numeric', month: 'short' }));
                
                const found = historyData.find(h => {
                    const hDate = new Date(h.date);
                    return hDate.getDate() === d.getDate() && hDate.getMonth() === d.getMonth();
                });
                
                msgData.push(found ? found.messages : 0);
                voiceData.push(found ? (found.voiceSeconds / 60).toFixed(0) : 0);
            }

            const options = {
                series: [{ name: 'Сообщения', data: msgData }, { name: 'Войс (мин)', data: voiceData }],
                chart: { type: 'area', height: 220, background: 'transparent', toolbar: { show: false }, fontFamily: 'Montserrat' },
                colors: ['#5865F2', '#ffd700'],
                dataLabels: { enabled: false },
                stroke: { curve: 'smooth', width: 2 },
                fill: { type: 'gradient', gradient: { opacityFrom: 0.4, opacityTo: 0.05 } },
                theme: { mode: 'dark' },
                xaxis: { categories: days, labels: { style: { colors: '#888' } }, axisBorder: { show: false }, axisTicks: { show: false } },
                yaxis: { show: false },
                grid: { show: false },
                legend: { show: false }
            };
            
            const chart = new ApexCharts(document.querySelector("#activityChart"), options);
            chart.render();
        }

        async function loadComments(page) {
            const container = document.getElementById('commentsList');
            try {
                const res = await fetch(`/api/profile/comments/${targetUserId}?page=${page}`);
                const data = await res.json();

                if (data.success) {
                    currentWallPage = data.pagination.current;
                    document.getElementById('wallHeader').innerHTML = `<i class="fas fa-comments"></i> Стена (${data.pagination.count})`;
                    if (data.comments.length > 0) {
                        container.innerHTML = data.comments.map(c => {
const avaUrl = c.authorAvatar 
    ? `/img/proxy/avatar/${c.authorId}/${c.authorAvatar}` 
    : '/assets/img/avatars/default_avatar.png';
                            const safeText = c.comment.replace(/&/g, "&amp;").replace(/</g, "&lt;");
                            return `
                                <div class="wall-item">
                                    <a href="/profile/${c.authorId}"><img src="${avaUrl}" class="wall-ava" onerror="this.src='/assets/img/avatars/default_avatar.png'"></a>
                                    <div class="wall-bubble">
                                        <div class="wall-meta">
                                            <a href="/profile/${c.authorId}" class="wall-author">${c.authorUsername}</a>
                                            <span class="wall-date">${new Date(c.timestamp).toLocaleDateString()}</span>
                                        </div>
                                        <div class="wall-text">${safeText}</div>
                                    </div>
                                </div>
                            `;
                        }).join('');
                    } else {
                        container.innerHTML = '<div class="wall-empty">Здесь пока тихо...</div>';
                    }
                    renderPagination(data.pagination.current, data.pagination.total);
                }
            } catch (e) { container.innerHTML = 'Ошибка загрузки'; }
        }

        function renderPagination(current, total) {
            const p = document.getElementById('wallPagination');
            if(total <= 1) { p.style.display = 'none'; return; }
            p.style.display = 'flex';
            let html = `<button class="page-btn" onclick="loadComments(${current - 1})" ${current === 1 ? 'disabled' : ''}>&lt;</button>`;
            html += `<span style="color:#888; font-weight:600;"> ${current} / ${total} </span>`;
            html += `<button class="page-btn" onclick="loadComments(${current + 1})" ${current === total ? 'disabled' : ''}>&gt;</button>`;
            p.innerHTML = html;
        }

        async function postComment() {
            const input = document.getElementById('wallComment');
            const text = input.value;
            if (!text.trim()) return;

            try {
                const res = await fetch('/api/profile/comment', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ targetUserId: targetProfileId, text: text })
                });
                const data = await res.json();
                if (data.success) {
                    input.value = '';
                    loadComments(1);
                } else {
                    if (typeof Modal !== 'undefined') Modal.alert("Ошибка", data.error, "error");
                    else alert(data.error);
                }
            } catch (e) { alert("Ошибка сети"); }
        }

        async function interactUser(type) {
             try {
                const res = await fetch(`/api/user/${targetUserId}/${type}`, { method: 'POST' });
                const data = await res.json();
                if (data.success) {
                    if (typeof Modal !== 'undefined') Modal.alert("Успешно", data.message, "success");
                    else alert(data.message);
                } else {
                    if (typeof Modal !== 'undefined') Modal.alert("Ошибка", data.error, "error");
                    else alert(data.error);
                }
            } catch (e) { alert("Ошибка"); }
        }
        
        const wallInput = document.getElementById('wallComment');
        if(wallInput) {
            wallInput.addEventListener('keypress', function (e) {
                if (e.key === 'Enter') postComment();
            });
        }
    </script>
</body>
</html>