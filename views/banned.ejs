<!DOCTYPE html>
<html lang="ru">
<head>
    <%- include('partials/head', { title: 'BANNED' }) %>
    <style>
        body { 
            background: #050505; 
            color: #fff; 
            font-family: 'Montserrat', sans-serif; 
            display: flex; 
            align-items: center; 
            justify-content: center; 
            min-height: 100vh; 
            margin: 0;
            overflow-y: auto; 
        }
        
        .ban-container { 
            text-align: center; 
            width: 100%;
            max-width: 600px; 
            padding: 40px; 
            background: #111; 
            border: 1px solid #333; 
            border-radius: 24px; 
            box-shadow: 0 0 100px rgba(255, 0, 0, 0.15); 
            position: relative; 
            margin: 20px;
            box-sizing: border-box; 
        }

        .ban-icon { 
            font-size: 5rem; 
            color: #ef4444; 
            margin-bottom: 20px; 
            animation: pulse 2s infinite; 
        }

        h1 { 
            font-size: 2.5rem; 
            font-weight: 900; 
            margin-bottom: 10px; 
            text-transform: uppercase; 
            letter-spacing: 2px; 
            line-height: 1.1;
        }

        p { 
            color: #aaa; 
            font-size: 1.1rem; 
            margin-bottom: 30px; 
            line-height: 1.6;
        }

        .ban-reason { 
            background: rgba(239, 68, 68, 0.1); 
            border: 1px solid rgba(239, 68, 68, 0.3); 
            padding: 15px; 
            border-radius: 12px; 
            color: #ef4444; 
            font-weight: 700; 
            margin-bottom: 30px; 
            font-family: monospace; 
            word-break: break-word; 
        }

        .appeal-form {
            text-align: left;
            margin-bottom: 30px;
            background: #1a1a1a;
            padding: 25px;
            border-radius: 16px;
            border: 1px solid #333;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-label {
            display: block;
            color: #888;
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 8px;
            font-weight: 700;
        }

        .form-input, .form-textarea {
            width: 100%;
            background: #0a0a0a;
            border: 1px solid #333;
            color: #fff;
            padding: 12px 15px;
            border-radius: 8px;
            font-family: inherit;
            font-size: 0.95rem;
            transition: 0.3s;
            box-sizing: border-box; 
        }

        .form-input:focus, .form-textarea:focus {
            outline: none;
            border-color: #ef4444;
            box-shadow: 0 0 15px rgba(239, 68, 68, 0.2);
        }

        .form-textarea {
            height: 100px;
            resize: vertical;
        }

        .btn-logout { 
            display: inline-block; 
            background: transparent; 
            color: #666; 
            padding: 12px 30px; 
            border-radius: 50px; 
            text-decoration: none; 
            font-weight: 600; 
            transition: 0.3s; 
            font-size: 0.9rem;
            border: 1px solid #333;
        }
        .btn-logout:hover { 
            border-color: #fff; 
            color: #fff; 
        }

        .btn-submit {
            width: 100%;
            background: #fff;
            color: #000;
            padding: 14px;
            border: none;
            border-radius: 12px;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 1px;
            cursor: pointer;
            transition: 0.3s;
            margin-top: 10px;
        }
        .btn-submit:hover {
            background: #ef4444;
            color: #fff;
            box-shadow: 0 5px 20px rgba(239, 68, 68, 0.4);
            transform: translateY(-2px);
        }

        .pending-status {
            background: rgba(255, 193, 7, 0.1);
            border: 1px solid rgba(255, 193, 7, 0.3);
            color: #ffc107;
            padding: 20px;
            border-radius: 16px;
            margin-bottom: 30px;
        }

        @keyframes pulse { 0% { transform: scale(1); opacity: 1; } 50% { transform: scale(1.1); opacity: 0.7; } 100% { transform: scale(1); opacity: 1; } }

        @media (max-width: 600px) {
            .ban-container {
                padding: 30px 20px;
                margin: 15px;
                width: auto;
            }

            .ban-icon {
                font-size: 4rem;
            }

            h1 {
                font-size: 1.8rem;
            }

            p {
                font-size: 1rem;
            }

            .appeal-form {
                padding: 15px; 
            }

            .form-input, .form-textarea {
                font-size: 16px;
            }

            .btn-logout {
                width: 100%;
                box-sizing: border-box;
                margin-top: 10px;
            }
        }
    </style>
</head>
<body>
    <div class="ban-container">
        <div class="ban-icon"><i class="fas fa-ban"></i></div>
        <h1>Доступ Заблокирован</h1>
        <p>Ваш аккаунт был заблокирован на сервере Discord "Дача Зейна". Доступ к сайту ограничен.</p>
        
        <div class="ban-reason">
            Причина: <%= reason %>
        </div>

        <% if (hasPendingAppeal) { %>
            <div class="pending-status">
                <i class="fas fa-hourglass-half" style="font-size: 2rem; margin-bottom: 10px;"></i><br>
                <strong>Ваша апелляция на рассмотрении.</strong><br>
                <span style="font-size: 0.9rem; opacity: 0.8;">Администрация уже получила ваш запрос. Ожидайте решения.</span>
            </div>
        <% } else { %>
            <form id="appealForm" class="appeal-form">
                <div class="form-group">
                    <label class="form-label">Ваша версия (За что получили бан?)</label>
                    <input type="text" id="userReason" class="form-input" placeholder="Например: Нарушил правило 1.4..." required minlength="5">
                </div>

                <div class="form-group">
                    <label class="form-label">Почему нас стоит вас разбанить?</label>
                    <textarea id="userAppeal" class="form-textarea" placeholder="Я осознал ошибку и обещаю..." required minlength="10"></textarea>
                </div>

                <button type="submit" class="btn-submit">Отправить апелляцию</button>
            </form>
        <% } %>

        <a href="/auth/logout" class="btn-logout">Выйти из аккаунта</a>
    </div>

    <script nonce="<%= nonce %>">
        const form = document.getElementById('appealForm');
        if (form) {
            form.addEventListener('submit', async (e) => {
                e.preventDefault();
                
                const userReason = document.getElementById('userReason').value;
                const userAppeal = document.getElementById('userAppeal').value;

                const combinedText = `[Причина бана со слов игрока]:\n${userReason}\n\n[Оправдание]:\n${userAppeal}`;
                
                const btn = form.querySelector('.btn-submit');
                const originalBtnText = btn.innerText;
                btn.disabled = true;
                btn.innerText = "Отправка...";

                try {
                    const res = await fetch('/api/appeal', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ text: combinedText })
                    });
                    const data = await res.json();
                    
                    if (data.success) {
                        alert('Заявка успешно отправлена!');
                        location.reload();
                    } else {
                        alert('Ошибка: ' + data.error);
                        btn.disabled = false;
                        btn.innerText = originalBtnText;
                    }
                } catch (e) { 
                    alert('Ошибка сети'); 
                    btn.disabled = false;
                    btn.innerText = originalBtnText;
                }
            });
        }
    </script>
</body>
</html>