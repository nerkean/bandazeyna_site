<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Подача заявки | Дача Зейна</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700;800;900&family=Raleway:wght@400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/beta.css">
</head>
<body>
    
    <div class="bg-glow"></div>
    <div class="bg-grid"></div>

    <div class="login-wrapper">
        <div class="login-card wide" data-tilt>
            
            <div class="card-header">
                <div class="logo-icon small">
                    <i class="fas fa-file-contract"></i>
                </div>
                <h1>ЗАЯВКА НА ДОСТУП</h1>
            </div>

            <div class="requirements-box">
                <h3><i class="fas fa-tasks"></i> Требования к кандидатам:</h3>
                <ul>
                    <li><i class="fas fa-check-circle"></i> <b>50 уровень</b> активности в JuniperBot</li>
                    <li><i class="fas fa-check-circle"></i> Участник сервера с <b>2024 года</b></li>
                    <li><i class="fas fa-check-circle"></i> Баланс от <b>1,000 ⭐</b></li>
                    <li><i class="fas fa-check-circle"></i> Отсутствие грубых нарушений</li>
                </ul>
            </div>

            <p class="login-desc">Заполните форму ниже. Мы проверим ваш профиль автоматически.</p>

            <% if (typeof success !== 'undefined' && success) { %>
                <div class="success-message">
                    <i class="fas fa-check-double"></i> Заявка отправлена! Ожидайте решения в ЛС Discord.
                </div>
                <a href="/beta-login" class="btn-back">Вернуться назад</a>
            <% } else { %>
                <form action="/beta-apply" method="POST" class="login-form">
                    <div class="input-group">
                        <div class="input-icon"><i class="fab fa-discord"></i></div>
                        <input type="text" name="discordUsername" placeholder="Ваш Discord Tag (напр. user123)" required>
                    </div>
                    
                    <div class="input-group">
                        <div class="input-icon"><i class="fas fa-id-card"></i></div>
                        <input type="text" name="uid" placeholder="Ваш Discord ID" required>
                    </div>

                    <div class="input-group">
                        <textarea name="reason" placeholder="Почему вы хотите получить ранний доступ?" rows="3" required></textarea>
                    </div>
                    
                    <button type="submit" class="btn-submit">
                        ОТПРАВИТЬ АНКЕТУ <i class="fas fa-paper-plane"></i>
                    </button>
                </form>
                
                <div class="card-footer">
                    <a href="/beta-login" class="link-back">Отмена</a>
                </div>
            <% } %>

        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/vanilla-tilt/1.7.0/vanilla-tilt.min.js"></script>
    <script>
        VanillaTilt.init(document.querySelector(".login-card"), { max: 3, speed: 400, glare: true, "max-glare": 0.05 });
    </script>
</body>
</html>