<% 
    function getOptimizedAvatar(url) {
        if (!url) return '/assets/img/avatars/default_avatar.png';
        if (url.includes('cdn.discordapp.com')) {
            return url.replace(/\.(png|jpg|jpeg)(\?.*)?$/, '.webp?size=128');
        }
        return url;
    }
%>
<!DOCTYPE html>
<html lang="ru">
<head>
    <style>
        @font-face { font-family: 'Montserrat'; font-style: normal; font-weight: 400; font-display: swap; src: url('/assets/fonts/web/montserrat-v31-cyrillic_latin-regular.woff2') format('woff2'); }
        @font-face { font-family: 'Montserrat'; font-style: normal; font-weight: 700; font-display: swap; src: url('/assets/fonts/web/montserrat-v31-cyrillic_latin-700.woff2') format('woff2'); }
        
        /* База и Фон */
        body { 
            font-family: 'Montserrat', sans-serif; 
            margin: 0;
            background-color: #0f1115; 
            background-image: 
                radial-gradient(circle at 15% 50%, rgba(46, 204, 113, 0.05) 0%, transparent 25%),
                radial-gradient(circle at 85% 30%, rgba(52, 152, 219, 0.05) 0%, transparent 25%);
            color: #fff;
            overflow-y: scroll; 
        }

        /* Контейнер */
        .container { max-width: 1200px; margin: 0 auto; padding: 120px 20px 80px; }

        /* Шапка страницы (LCP элемент) */
        .page-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 40px; flex-wrap: wrap; gap: 20px; }
        .header-text h1 { font-size: 2.5rem; font-weight: 900; margin: 0; text-transform: uppercase; line-height: 1; }
        .header-text p { margin: 5px 0 0; color: #888; font-size: 0.95rem; }
        
        /* Кнопка создания */
        .create-btn-floating {
            background: linear-gradient(135deg, #2ecc71, #27ae60); color: white;
            border: none; padding: 12px 24px; border-radius: 50px;
            font-weight: 700; font-size: 1rem; cursor: pointer;
            box-shadow: 0 10px 20px rgba(46, 204, 113, 0.3);
            text-decoration: none; display: inline-flex; align-items: center; gap: 10px;
        }

        /* Фильтры */
        .filter-container { display: flex; gap: 10px; overflow-x: auto; padding-bottom: 15px; margin-bottom: 30px; }
        .filter-btn {
            background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.1);
            color: #aaa; padding: 8px 20px; border-radius: 20px; cursor: pointer;
            font-size: 0.9rem; font-family: 'Montserrat', sans-serif; font-weight: 600; white-space: nowrap;
        }
        .filter-btn.active { background: #fff; color: #000; transform: scale(1.05); }

        /* Сетка (Каркас) */
        .grid-container { 
            display: grid; grid-template-columns: repeat(auto-fill, minmax(340px, 1fr)); gap: 25px;
        }

        /* Доп. стили для ника (замена h4) */
        .tm-username { font-size: 1.1rem; font-weight: 700; color: #fff; margin-bottom: 2px; }
        
        /* Кнопка помощи */
        .help-btn { width: 32px; height: 32px; border-radius: 50%; background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.2); color: #aaa; display: flex; align-items: center; justify-content: center; cursor: pointer; }
    </style>

    <link rel="preload" href="/css/teammates.css" as="style" onload="this.onload=null;this.rel='stylesheet'">
    <noscript><link rel="stylesheet" href="/css/teammates.css"></noscript>

    <%- include('partials/head', { title: 'Поиск Тиммейтов' }) %>
</head>
<body>
    <%- include('partials/navbar') %>

    <main class="container">
        
        <div class="page-header">
            <div class="header-text">
                <div style="display: flex; align-items: center; gap: 15px;">
                    <h1>LFG <span style="color: #2ecc71;">Board</span></h1>
                    <button class="help-btn" onclick="showLfgRules()" title="Правила LFG" aria-label="Правила">
                        <i class="fas fa-question"></i>
                    </button>
                </div>
                <p>Заявки удаляются автоматически через 30 минут</p>
            </div>

            <% if (user) { %>
                <button class="create-btn-floating" onclick="openLfgModal()">
                    <i class="fas fa-plus-circle"></i> 
                    <%= myRequest ? 'Моя заявка' : 'Создать объявление' %>
                </button>
            <% } else { %>
                <a href="/auth/discord" class="create-btn-floating" style="background: #5865F2; text-decoration: none;">
                    <i class="fab fa-discord"></i> Войти
                </a>
            <% } %>
        </div>

        <div class="filter-container">
            <button class="filter-btn active" onclick="setFilter('ALL', this)">Все</button>
            <button class="filter-btn" onclick="setFilter('MACRO', this)">🤖 Макро</button>
            <button class="filter-btn" onclick="setFilter('FARM', this)">🐝 Фарм</button>
            <button class="filter-btn" onclick="setFilter('PUFFSHROOMS', this)">🍄 Puffs</button>
            <button class="filter-btn" onclick="setFilter('ROBO', this)">🤖 Robo</button>
            <button class="filter-btn" onclick="setFilter('OTHER', this)">❓ Другое</button>
        </div>

        <div class="grid-container" id="cardsContainer">
            <% requests.forEach((req, index) => { %>
                <div class="tm-card hive-<%= req.hiveColor %>" data-type="<%= req.activityType %>">
                    
                    <div class="card-top">
                        <div class="user-wrap">
                            <img src="<%= getOptimizedAvatar(req.avatar) %>" 
     class="tm-avatar" 
     alt="<%= req.username %>" 
     width="45" height="45"
     loading="<%= index < 4 ? 'eager' : 'lazy' %>"
     fetchpriority="<%= index < 4 ? 'high' : 'auto' %>"
     decoding="async"
     onerror="this.src='/assets/img/avatars/default_avatar.png'">
                            
                            <div class="user-details">
                                <div class="tm-username"><%= req.username %></div>
                                <span class="hive-tag" style="color: <%= req.hiveColor === 'BLUE' ? '#3498db' : req.hiveColor === 'RED' ? '#e74c3c' : req.hiveColor === 'WHITE' ? '#ecf0f1' : '#9b59b6' %>">
                                    <%= req.hiveColor %> Hive
                                </span>
                            </div>
                        </div>

                        <div class="activity-badge act-<%= req.activityType %>">
                            <% if(req.activityType === 'MACRO') { %> <i class="fas fa-robot"></i> <span>Macro</span> <% } %>
                            <% if(req.activityType === 'FARM') { %> <i class="fas fa-leaf"></i> <span>Farm</span> <% } %>
                            <% if(req.activityType === 'STICKERS') { %> <i class="fas fa-sticky-note"></i> <span>Trade</span> <% } %>
                            <% if(req.activityType === 'PUFFSHROOMS') { %> <i class="fas fa-cloud"></i> <span>Puffs</span> <% } %>
                            <% if(req.activityType === 'ROBO') { %> <i class="fas fa-gamepad"></i> <span>Robo</span> <% } %>
                            <% if(req.activityType === 'OTHER') { %> <i class="fas fa-question-circle"></i> <span>Другое</span> <% } %>
                        </div>
                    </div>

                    <div class="tm-desc">
                        <%= req.description %>
                    </div>

                    <div class="card-footer">
                        <button class="discord-btn" onclick="copyDiscord('<%= req.username %>', this)">
                            <i class="fab fa-discord"></i> Copy Nick
                        </button>
                        
                        <span class="time-ago">
                            <% const diff = Math.floor((Date.now() - new Date(req.createdAt)) / 60000); %>
                            <%= diff < 1 ? 'Только что' : diff + ' мин. назад' %>
                        </span>
                    </div>
                </div>
            <% }) %>
        </div>

        <div id="emptyState" style="text-align: center; color: #555; margin-top: 50px; display: none;">
            <i class="far fa-clock" style="font-size: 3rem; margin-bottom: 10px; opacity: 0.5;"></i>
            <p>Активных заявок в этой категории нет.<br>Создай свою!</p>
        </div>

        <div class="pagination-wrapper" id="paginationControls"></div>
    </main>

    <% if (user) { %>
    <div class="modal-overlay" id="lfgModalOverlay">
        <div class="modal-window">
            <div class="modal-header">
                <h2><%= myRequest ? '✏️ Изменить заявку' : '✨ Новая заявка' %></h2>
                <button class="modal-close" onclick="closeLfgModal()">&times;</button>
            </div>
            
            <form id="lfgForm">
                <div class="form-group">
                    <label class="form-label">Цель поиска</label>
                    <select name="activityType" class="custom-input" required>
                        <option value="MACRO" <%= myRequest?.activityType === 'MACRO' ? 'selected' : '' %>>🤖 Макро (Macro)</option>
                        <option value="FARM" <%= myRequest?.activityType === 'FARM' ? 'selected' : '' %>>🐝 Фарм Мёда</option>
                        <option value="PUFFSHROOMS" <%= myRequest?.activityType === 'PUFFSHROOMS' ? 'selected' : '' %>>🍄 Puffshrooms</option>
                        <option value="ROBO" <%= myRequest?.activityType === 'ROBO' ? 'selected' : '' %>>🤖 Robo Challenge</option>
                        <option value="OTHER" <%= myRequest?.activityType === 'OTHER' ? 'selected' : '' %>>❓ Другое</option>
                    </select>
                </div>

                <div class="form-group">
                    <label class="form-label">Твой Улей</label>
                    <select name="hiveColor" class="custom-input" required>
                        <option value="BLUE" <%= myRequest?.hiveColor === 'BLUE' ? 'selected' : '' %>>🔵 Синий (Blue)</option>
                        <option value="RED" <%= myRequest?.hiveColor === 'RED' ? 'selected' : '' %>>🔴 Красный (Red)</option>
                        <option value="WHITE" <%= myRequest?.hiveColor === 'WHITE' ? 'selected' : '' %>>⚪ Белый (White)</option>
                        <option value="MIXED" <%= myRequest?.hiveColor === 'MIXED' ? 'selected' : '' %>>🟣 Смешанный (Mixed)</option>
                    </select>
                </div>

                <div class="form-group">
                    <label class="form-label">Описание (до 80 симв.)</label>
                    <input type="text" name="description" 
                           class="custom-input" 
                           placeholder="Например: Ищу +1 на макро на поле Pine Tree" 
                           maxlength="80" 
                           value="<%= myRequest?.description || '' %>"
                           required>
                </div>

                <div class="modal-actions">
                    <button type="submit" class="btn-submit">
                        <%= myRequest ? 'Сохранить изменения' : 'Опубликовать заявку' %>
                    </button>
                    
                    <% if (myRequest) { %>
                        <button type="button" class="btn-delete" onclick="deleteRequest()" title="Удалить заявку">
                            <i class="fas fa-trash-alt"></i>
                        </button>
                    <% } %>
                </div>
            </form>
        </div>
    </div>
    <% } %>

    <script nonce="<%= nonce %>">
        document.addEventListener('DOMContentLoaded', () => {
            const ITEMS_PER_PAGE = 12;
            let currentPage = 1;
            let currentFilter = 'ALL';
            const allCards = Array.from(document.querySelectorAll('.tm-card'));
            const paginationContainer = document.getElementById('paginationControls');
            const emptyState = document.getElementById('emptyState');

            function render() {
                const filtered = allCards.filter(card => {
                    return currentFilter === 'ALL' || card.dataset.type === currentFilter;
                });
                const totalPages = Math.ceil(filtered.length / ITEMS_PER_PAGE);
                if (currentPage > totalPages) currentPage = 1;
                if (totalPages === 0) currentPage = 1;
                const startIndex = (currentPage - 1) * ITEMS_PER_PAGE;
                const endIndex = startIndex + ITEMS_PER_PAGE;

                allCards.forEach(card => card.style.display = 'none');
                
                if (filtered.length === 0) {
                    emptyState.style.display = 'block';
                } else {
                    emptyState.style.display = 'none';
                    filtered.slice(startIndex, endIndex).forEach(card => {
                        card.style.display = 'flex';
                        card.style.opacity = '0';
                        card.style.animation = 'fadeIn 0.3s forwards';
                    });
                }
                renderPagination(totalPages);
            }

            function renderPagination(totalPages) {
                paginationContainer.innerHTML = '';
                if (totalPages <= 1) return;
                const prevBtn = document.createElement('button');
                prevBtn.className = 'page-btn';
                prevBtn.innerHTML = '<i class="fas fa-chevron-left"></i>';
                prevBtn.disabled = currentPage === 1;
                prevBtn.onclick = () => { currentPage--; render(); window.scrollTo({top: 0, behavior: 'smooth'}); };
                paginationContainer.appendChild(prevBtn);

                for (let i = 1; i <= totalPages; i++) {
                    if (i === 1 || i === totalPages || (i >= currentPage - 1 && i <= currentPage + 1)) {
                        const btn = document.createElement('button');
                        btn.className = `page-btn ${i === currentPage ? 'active' : ''}`;
                        btn.innerText = i;
                        btn.onclick = () => { currentPage = i; render(); window.scrollTo({top: 0, behavior: 'smooth'}); };
                        paginationContainer.appendChild(btn);
                    } else if ((i === currentPage - 2 && currentPage > 3) || (i === currentPage + 2 && currentPage < totalPages - 2)) {
                        const span = document.createElement('span'); span.innerText = '...'; span.style.color = '#666'; span.style.paddingTop = '10px';
                        paginationContainer.appendChild(span);
                    }
                }
                const nextBtn = document.createElement('button');
                nextBtn.className = 'page-btn';
                nextBtn.innerHTML = '<i class="fas fa-chevron-right"></i>';
                nextBtn.disabled = currentPage === totalPages;
                nextBtn.onclick = () => { currentPage++; render(); window.scrollTo({top: 0, behavior: 'smooth'}); };
                paginationContainer.appendChild(nextBtn);
            }

            window.setFilter = (type, btn) => {
                document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
                btn.classList.add('active');
                currentFilter = type;
                currentPage = 1;
                render();
            };
            render();
        });

        function openLfgModal() { document.getElementById('lfgModalOverlay')?.classList.add('active'); }
        function closeLfgModal() { document.getElementById('lfgModalOverlay')?.classList.remove('active'); }
        document.getElementById('lfgModalOverlay')?.addEventListener('click', (e) => {
            if(e.target.id === 'lfgModalOverlay') closeLfgModal();
        });

        function copyDiscord(username, btn) {
            navigator.clipboard.writeText(username).then(() => {
                const originalHtml = btn.innerHTML;
                btn.innerHTML = '<i class="fas fa-check"></i> Скопировано';
                btn.style.background = '#2ecc71';
                btn.style.transition = '0.2s';
                setTimeout(() => {
                    btn.innerHTML = originalHtml;
                    btn.style.background = '';
                }, 2000);
            }).catch(err => { console.error('Ошибка копирования:', err); });
        }

        const form = document.getElementById('lfgForm');
        if (form) {
            form.addEventListener('submit', async (e) => {
                e.preventDefault();
                const btn = form.querySelector('button[type="submit"]');
                const originalText = btn.innerText;
                btn.innerText = 'Загрузка...'; btn.disabled = true;
                const data = Object.fromEntries(new FormData(e.target));
                try {
                    const res = await fetch('/teammates/create', { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify(data) });
                    const result = await res.json();
                    if (result.success) { location.reload(); } 
                    else { alert(result.error || 'Ошибка'); btn.innerText = originalText; btn.disabled = false; }
                } catch (err) { alert('Ошибка сети'); btn.innerText = originalText; btn.disabled = false; }
            });
        }

        async function deleteRequest() {
            if (typeof Modal !== 'undefined') {
                const isConfirmed = await Modal.confirm('Удаление заявки', 'Вы уверены?', 'warning');
                if (!isConfirmed) return;
            } else { if (!confirm('Удалить заявку?')) return; }
            const btn = document.querySelector('.btn-delete');
            if (btn) btn.disabled = true;
            try { await fetch('/teammates/delete', { method: 'POST' }); location.reload(); } catch (e) { console.error(e); alert('Ошибка удаления'); if (btn) btn.disabled = false; }
        }

        const LFG_RULES = `
            <ul style="text-align: left; list-style: none; padding: 0; font-size: 0.95rem; color: #ccc;">
                <li style="margin-bottom: 10px;">🕒 <b>Срок жизни:</b> каждая заявка автоматически удаляется через <b>30 минут</b>.</li>
                <li style="margin-bottom: 10px;">⚖️ <b>Правила:</b> запрещено нарушать правила сервера.</li>
                <li style="margin-bottom: 10px;">🤝 <b>Уважение:</b> удаляйте неактуальные заявки.</li>
            </ul>
        `;

        function showLfgRules() {
            if (window.Modal) Modal.alert('📋 Правила LFG Board', LFG_RULES, 'info');
            else alert('Правила:\n1. Заявки живут 30 мин.');
        }

        document.addEventListener('DOMContentLoaded', () => {
            const hasSeenRules = localStorage.getItem('lfg_rules_seen');
            if (!hasSeenRules) {
                setTimeout(() => { showLfgRules(); localStorage.setItem('lfg_rules_seen', 'true'); }, 1000);
            }
        });
    </script>
    <style>@keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }</style>
</body>
</html>