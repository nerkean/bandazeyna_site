<!DOCTYPE html>
<html lang="ru">
<head>
    <%- include('partials/head', { title: 'Инвентарь | Дача Зейна' }) %>
    <link rel="stylesheet" href="/css/inventory.css">
</head>
<body>
    <%- include('partials/navbar') %>

    <div class="inv-wrapper">
        
        <header class="inv-header">
            <div class="inv-title">
                <h1>🎒 Мой Инвентарь</h1>
                <span class="inv-count"><%= inventory.length %> предметов</span>
            </div>

            <div class="inv-tabs">
                <button class="tab-btn active" onclick="filterInv('all', this)">Все</button>
                <button class="tab-btn" onclick="filterInv('Лутбокс', this)">Лутбоксы</button>
                <button class="tab-btn" onclick="filterInv('Расходник', this)">Расходники</button>
                <button class="tab-btn" onclick="filterInv('Косметика', this)">Косметика</button>
                <button class="tab-btn" onclick="filterInv('Ресурс', this)">Ресурсы</button>
            </div>

            <div class="inv-stats">
                <div class="inv-stat">
                    <span>Баланс:</span>
                    <strong style="color: #ffd700;"><%= Math.floor(profile.stars).toLocaleString() %> ⭐</strong>
                </div>
                <a href="/shop" class="btn-secondary"><i class="fas fa-shopping-cart"></i> В Магазин</a>
            </div>
        </header>

        <div class="inv-grid-container">
            <% if (inventory.length > 0) { %>
                <div class="inv-grid" id="invGrid">
                    <% inventory.forEach((slot, index) => { 
                        // Определяем редкость (можно доработать логику)
                        let rarity = 'common';
                        const cat = slot.details.category;
                        const name = slot.details.name.toLowerCase();

                        if (name.includes('легендар') || name.includes('мифич') || cat === 'Лутбокс') rarity = 'legendary';
                        else if (name.includes('редкий') || cat === 'Украшение профиля') rarity = 'epic';
                        else if (name.includes('необычный')) rarity = 'rare';
                    %>
                        <div class="inv-card <%= rarity %>" 
                             onclick="openItemModal(<%= index %>)" 
                             data-category="<%= cat %>">
                            
                            <div class="inv-card-bg"></div>
                            
                            <div class="inv-icon-wrapper">
                                <% if (slot.details.imageUrl_web) { %>
                                    <img src="<%= slot.details.imageUrl_web %>" class="inv-img">
                                <% } else { %>
                                    <div class="inv-emoji"><%= slot.details.emoji %></div>
                                <% } %>
                                <span class="inv-amount">x<%= slot.quantity %></span>
                            </div>
                            
                            <div class="inv-info">
                                <div class="inv-name"><%= slot.details.name %></div>
                                <div class="inv-type"><%= cat %></div>
                            </div>
                        </div>
                    <% }) %>
                </div>
            <% } else { %>
                <div class="empty-inv">
                    <div class="empty-icon"><i class="fas fa-box-open"></i></div>
                    <h3>Пусто...</h3>
                    <p>Кажется, вы еще ничего не добыли.</p>
                    <a href="/shop" class="btn-primary">Перейти к покупкам</a>
                </div>
            <% } %>
        </div>
    </div>

    <div id="itemModal" class="modal-overlay">
        <div class="modal-content">
            <button class="close-btn" onclick="closeModal()"><i class="fas fa-times"></i></button>
            
            <div class="modal-visual" id="mVisual">
                </div>

            <div class="modal-body">
                <h2 id="mName">Название</h2>
                <span class="modal-type" id="mType">Тип</span>
                <p id="mDesc" class="modal-desc">Описание...</p>
                
                <div class="modal-info-row">
                    <span>В наличии:</span>
                    <strong id="mQty" style="color: #fff;">x0</strong>
                </div>

                <div class="modal-actions" id="mActions"></div>
            </div>
        </div>
    </div>

    <%- include('partials/footer') %>

    <script>
        const inventoryData = <%- JSON.stringify(inventory) %>;
        let currentItem = null;

        // --- ФИЛЬТРАЦИЯ ---
        function filterInv(category, btn) {
            const cards = document.querySelectorAll('.inv-card');
            const buttons = document.querySelectorAll('.tab-btn');

            if (btn) {
                buttons.forEach(b => b.classList.remove('active'));
                btn.classList.add('active');
            }

            cards.forEach(card => {
                const cat = card.getAttribute('data-category');
                let match = false;

                if (category === 'all') match = true;
                else if (category === 'Косметика' && (cat === 'Украшение профиля' || cat === 'Титул')) match = true;
                else if (cat.includes(category)) match = true;

                card.style.display = match ? 'flex' : 'none';
            });
        }

        // --- МОДАЛКА ---
        function openItemModal(index) {
            const item = inventoryData[index];
            currentItem = item;

            document.getElementById('mName').innerText = item.details.name;
            document.getElementById('mDesc').innerText = item.details.description || 'Нет описания';
            document.getElementById('mType').innerText = item.details.category;
            document.getElementById('mQty').innerText = 'x' + item.quantity;

            // Визуал
            const visualDiv = document.getElementById('mVisual');
            if (item.details.imageUrl_web) {
                visualDiv.innerHTML = `<img src="${item.details.imageUrl_web}" class="modal-img">`;
            } else {
                visualDiv.innerHTML = `<div class="modal-emoji">${item.details.emoji}</div>`;
            }

            // Действия
            const actionsDiv = document.getElementById('mActions');
            actionsDiv.innerHTML = '';

            const cat = item.details.category;
            // Список категорий, которые можно использовать
            const usableCats = ['Лутбокс', 'Расходник', 'Набор ресурсов'];
            const isUsable = usableCats.includes(cat) || item.details.isUsable;

            if (isUsable) {
                const btnLabel = (cat === 'Лутбокс' || cat === 'Набор ресурсов') ? 'Открыть' : 'Использовать';
                const btn = document.createElement('button');
                btn.className = 'btn-primary full-width';
                btn.innerHTML = `<i class="fas fa-play"></i> ${btnLabel}`;
                btn.onclick = () => useItem(item.itemId);
                actionsDiv.appendChild(btn);
            } else {
                const info = document.createElement('div');
                info.className = 'unusable-badge';
                info.innerText = 'Предмет пассивного действия';
                actionsDiv.appendChild(info);
            }

            document.getElementById('itemModal').classList.add('active');
        }

        function closeModal() {
            document.querySelectorAll('.modal-overlay').forEach(el => el.classList.remove('active'));
        }

        async function useItem(itemId) {
            if (typeof Modal !== 'undefined') {
                const confirmed = await Modal.confirm("Использование", "Вы уверены, что хотите использовать этот предмет?");
                if (!confirmed) return;
            } else if (!confirm('Вы уверены?')) return;

            closeModal(); // Сначала закрываем карточку предмета

            try {
                const res = await fetch('/api/inventory/use', {
                    method: 'POST',
                    headers: {'Content-Type': 'application/json'},
                    body: JSON.stringify({ itemId: itemId, quantity: 1 })
                });
                const result = await res.json();

                if (result.success) {
                    if (result.type === 'lootbox') {
                        // Для лутбоксов показываем красивый список наград через Modal.alert (или кастом)
                        let rewardText = result.rewards.map(r => `${r.emoji} ${r.name} x${r.quantity}`).join('<br>');
                        if (typeof Modal !== 'undefined') {
                            await Modal.alert("🎉 Награда!", `<div style="text-align:left">${rewardText}</div>`, "success");
                        } else {
                            alert("Вы получили:\n" + result.rewards.map(r => `${r.name} x${r.quantity}`).join('\n'));
                        }
                        location.reload();
                    } else {
                        if (typeof Modal !== 'undefined') await Modal.alert("Успешно", result.message, "success");
                        else alert(result.message);
                        location.reload();
                    }
                } else {
                    if (typeof Modal !== 'undefined') Modal.alert("Ошибка", result.error, "error");
                    else alert('Ошибка: ' + result.error);
                }
            } catch (e) {
                console.error(e);
                if (typeof Modal !== 'undefined') Modal.alert("Ошибка", "Ошибка сервера", "error");
            }
        }
        
        // Закрытие по клику на фон
        window.onclick = function(e) {
            if (e.target.classList.contains('modal-overlay')) closeModal();
        }
    </script>
</body>
</html>